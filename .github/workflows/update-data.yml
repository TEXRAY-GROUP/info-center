name: 更新資料

on:
  schedule:
    # 一天兩次,週一至週五(台銀假日不掛牌)。
    # 刻意避開每小時開頭的尖峰:GitHub 排程在整點附近常延遲數十分鐘甚至跳過。
    - cron: '37 0 * * 1-5'   # UTC 00:37 = 台北 08:37,上班前先有前一日牌告
    - cron: '23 9 * * 1-5'   # UTC 09:23 = 台北 17:23,台銀當日牌告已出,順便補跑早上漏掉的
  workflow_dispatch:        # 也可在 Actions 頁面手動執行

permissions:
  contents: write

concurrency:
  group: update-data
  cancel-in-progress: false

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: 抓取匯率(臺灣銀行牌告)
        run: python scripts/fetch_rates.py

      # 燃油附加費:DHL 自動抓,FedEx 讀 data/fuel_manual.json(人工每週維護)
      # 產出的 data/fuel.json 供內部運費試算工具跨域取用
      - name: 抓取燃油附加費
        run: python scripts/fetch_fuel.py

      # 未來新增積木時,在這裡加一行 run: python scripts/fetch_<block>.py

      - name: 有變動才 commit
        run: |
          git config user.name  "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add data/
          if git diff --staged --quiet; then
            echo "資料無變動,略過 commit"
          else
            git commit -m "資料自動更新 $(TZ=Asia/Taipei date '+%Y-%m-%d %H:%M')"
            git push
          fi
